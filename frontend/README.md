# FlavorJourney Frontend

## Cài đặt nhanh

### 1. Cài đặt dependencies

```bash
npm install
```

### 2. Cấu hình .env (tùy chọn)

Tạo file `.env`:

```env
VITE_API_URL=http://localhost:3000
```

### 3. Chạy ứng dụng

```bash
npm run dev
```

Server chạy tại: `http://localhost:5173`
